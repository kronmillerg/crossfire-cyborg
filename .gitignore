# This directory tends to have player-specific config information, which isn't
# part of the code and doesn't really belong in the git repo.
data/

# Compiled Python files.
*.py[cod]

# vim .swp files, so I don't accidentally add them with a "git add ."
.sw[nop]
.*.sw[op]
